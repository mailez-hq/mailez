# Dev-mode route for the mailez admin UI: in the hybrid dev setup the admin
# app runs on the host (localhost:3000), so proxy to host.docker.internal
# instead of a container named "admin".
location /admin {
    include /etc/nginx/proxy.conf;
    proxy_pass http://host.docker.internal:3000;
}
