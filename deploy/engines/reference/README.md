# engines/reference

Placeholder for the reference server mail engine.

When integrated, this directory will hold the engine component Dockerfiles
(built and tagged by `backend/cmd/build-images`) and the corresponding
`docker-compose.reference.yml` override in `deploy/`.

Current status: evaluation complete, integration deferred until reference server
reaches 1.0 or a concrete deployment need appears (see
`docs/engines/reference-evaluation.md`).
