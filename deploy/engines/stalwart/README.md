# engines/stalwart

Placeholder for the Stalwart mail engine.

When integrated, this directory will hold the engine component Dockerfiles
(built and tagged by `backend/cmd/build-images`) and the corresponding
`docker-compose.stalwart.yml` override in `deploy/`.

Current status: evaluation complete, integration deferred until Stalwart
reaches 1.0 or a concrete deployment need appears (see
`docs/engines/stalwart-evaluation.md`).
