try_fallback = false;
use_esld = false;
allow_username_mismatch = true;
use_vault = true;
vault_url = "http://{{ .BackendAddress }}:8080/stack/rspamd/vault{{ if .StackSecret }}?stack_secret={{ urlquery .StackSecret }}{{ end }}";
vault_token = "mailez";
.include(try=true,priority=1,duplicate=merge) "/overrides/dkim_signing.conf"
