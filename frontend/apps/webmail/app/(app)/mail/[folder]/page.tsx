// Folder list view. The three-pane UI lives in (app)/mail/layout.tsx (MailView),
// which keeps the list mounted across transitions; this leaf mainly exists to
// form the /mail/[folder] route segment.
export default function MailFolderPage() {
  return null;
}
