{
  "name": "@mailez/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API contract types consumed by the admin and webmail apps. The source of truth will move to types generated from the backend OpenAPI spec.",
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "scripts": {
    "gen:types": "node scripts/gen-types.mjs && openapi-typescript src/openapi.json -o src/generated.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "swagger2openapi": "^7.0.8"
  }
}
